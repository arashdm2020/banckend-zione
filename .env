   # App settings
   APP_ENV=development
   APP_PORT=8080
   APP_HOST=0.0.0.0
   APP_URL=http://localhost:8080

   # Database settings
   DB_HOST=mysql
   DB_PORT=3306
   DB_NAME=zione_db
   DB_USER=zione_user
   DB_PASSWORD=zione_password

   # JWT settings
   JWT_SECRET=your_jwt_secret_key
   JWT_EXPIRY=24h
   JWT_REFRESH_EXPIRY=168h

   # CORS
   CORS_ALLOWED_ORIGINS=http://localhost:3000

   # MySQL root password (for Docker setup)
   MYSQL_ROOT_PASSWORD=root_password